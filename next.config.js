/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'firebasestorage.googleapis.com',
      'pw-admin-panel.firebasestorage.app'
    ],
  },
};

export default nextConfig;
